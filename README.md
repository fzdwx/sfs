# sfs

static file server

```shell
Usage of sfs:
  -d string
        Directory to serve (default ".")
  -p int
        Port to listen on (default 8080)
```

```shell
go install github.com/fzdwx/sfs@main
```
